{
  "name": "rentlify_solutions",
  "version": "1.0.0",
  "description": "Rentlify Solutions restaurant platform monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @rentlify/authorization-contracts build && pnpm --parallel --filter rentlify_solutions_web --filter rentlify_solutions_server dev",
    "build": "pnpm --recursive --if-present build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm --recursive --if-present lint",
    "typecheck": "pnpm --recursive --if-present typecheck",
    "validate": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm build"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.33.2",
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
